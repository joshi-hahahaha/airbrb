import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#EB4C60',
    },
    // 你可以在这里添加更多的颜色
  },
  // 你也可以添加其他主题选项
});

export default theme;
